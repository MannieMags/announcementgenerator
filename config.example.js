/**
 * API Configuration Template
 * --------------------------
 * Copy this file to config.js and fill in your key.
 * config.js is gitignored and will NEVER be committed.
 *
 * For the live GitHub Pages site the key is injected automatically
 * by the GitHub Actions deploy workflow using a repository secret.
 */
window.PANGEA_CONFIG = {
    apiKey: 'YOUR_PANGEA_API_KEY_HERE'
};
